#include<stdio.h>
#include<string.h>  // use this header file to use the inbuid strings function.


int main(int argc, char const *argv[])
{

/*

    // there are many inbuild function in c. we will all the function here and know what it does.
    // you can use the <string.h> hearder file to use this functions.

    // there are a lot more but this are the basics.

    char string1 [] = "Amit";
    char srting2 [] = "Bhagat";

    strlwr(string1);     // will  convert the string to lower case. Example output: amit

    strupr(string1);    // will connver the string to upper case. Example output: AMIT

    strcat(string1, srting2);   // append string 2 too end of string 1. Example output: AmitBhagat

    strncat(string1, srting2 , 1);    // append n character from string2 to string1. Example output: AmitB

    strcpy(string1, srting2);   // copy string2 to string1. Example output: bhagat 

    strncpy(string1, string2, 3); //  copy n character of string2 to string1. Example output: Bhat

    strset(string1, '?');   // sets all character of a string to a given character. Example output: ????

    strnset(string1, '*', 1);   // set first n character of a string to a given character. Example output: *mit

    strrev(string1); // reverse a string Example output: tima

    printf("%s",string1);

    // This sets of fuction return a set of integers

    int result = strlen(string1); // return string length as int. Example output: 4

    int result = strcmp(string1, srting2); // string compares all character. Example output: false // this fuction returns 0 if true and 1 if false.

    int result = strncmp(string1, srting2, 1);  // string compare n character. Example output: false // this fuction returns 0 if true and 1 if false.

    int result = strcmpi(string1, srting2); // string compare all (ignore case). Examle output: // does the same things as strcmp & ignores case sensitivity.

    int result = strnicmp(string1, srting2, 1); // strng compares n character (ignore case). Example output: // does the same things as strcmp & ignores case sensitivity.


    printf("%d",result);
    
*/

    return 0;
}
