#include<stdio.h>

int main(int argc, char const *argv[])
{
    /* 
    
        // continue = skips rest of code & forces the next iteration of the loop
        // break = exits a loop/switch

        // ------------ Example Continue --------- //

        // this code will print number will print number till 20 but will skip the number 13.

        for (int i = 0; i <= 20; i++)
        {
            if(i == 13)
            {
                continue; // skips a step and continue the code
            }
            printf("%d\n",i)
        }


        // -------------------------------------//

        //----------- Example break --------------//

        // this code will print number will print number till 20 but will stop once it reaches 13.
    
        for (int i = 0; i <= 20; i++)
        {
            if(i == 13)
            {
                break; // exits a the code
            }
            printf("%d\n",i)
        }

        // -------------------------------------//


     */
    return 0;
}
