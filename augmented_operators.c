#include<stdio.h>

int main(int argc, char const *argv[])
{
    /*
    
    Augmented assiment operators = Used to replace a statement where an operator takes a variable as 
    one of its argument and then assign the result back to the same varaible.
    
    */

   // ------------ EXAMPLE ------------

   int x = 10;

   // x = x + 2;
   // x+=2;

   // x = x - 3;
  // x -=3;

  // x = x * 5;
  // x*=5

  // x = x / 5;
  // x/=5 ;

  // x = x % 2;
  // x %= 2;

printf("%d",x);

    return 0;
}
