# include<stdio.h>

int main(int argc, char const *argv[])
{
    
    // constant = A fixed value that cannot be altered by the program during its execuation 

    const float PI = 3.14;

    printf("The starting value of pie:%f",PI); 
    
    
    // you can use %2.f to show only 2 value after the decimal point or any number depending on how many 
    // numbers you want to show . *NOTE = float has limit of number it can store. USE double to store larger values. 

    return 0;
}
